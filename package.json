{
  "name": "qwen-team-workspace",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/team-backend", 
    "packages/team-ui/server",
    "packages/team-ui/client",
    "packages/team-core-agent",
    "packages/core",
    "packages/cli",
    "packages/sdk-typescript"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "clean": "rm -rf packages/*/dist packages/*/node_modules"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
