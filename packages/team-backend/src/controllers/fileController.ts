import { Request, Response } from "express";

export const listFiles = async (req: Request, res: Response): Promise<void> => { res.json({ success: true }); };
export const uploadFile = async (req: Request, res: Response): Promise<void> => { res.json({ success: true }); };
export const downloadFile = async (req: Request, res: Response): Promise<void> => { res.json({ success: true }); };
export const deleteFile = async (req: Request, res: Response): Promise<void> => { res.json({ success: true }); };
export const searchFiles = async (req: Request, res: Response): Promise<void> => { res.json({ success: true }); };
